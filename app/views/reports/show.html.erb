<div class="show-item">
  <%= render @report %>
</div>

<%= render partial: 'shared/comments/compound', locals: { commentable: @report } %>

<nav class="page-nav">
  <%= link_to t('views.common.edit', name: i18n_pluralize(Report.model_name.human.downcase)), edit_report_path(@report) %>
  <%= link_to t('views.common.back', name: i18n_pluralize(Report.model_name.human.downcase)), reports_path %>
  <%= button_to t('views.common.destroy', name: Report.model_name.human.downcase), @report, method: :delete %>
</nav>
